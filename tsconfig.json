{
  "compilerOptions": {
    /* === Basic Options === */
    "target": "ES2022",           // ← Compile to ES2022 (stable, Node 18+ supports 100%)
    "module": "ESNext",            // ← ES module system (import/export, not require)
    "lib": ["ES2022"],             // ← Libraries TypeScript should know (async/await, ??, ?., etc.)

    /* === Module Resolution === */
    "moduleResolution": "node",    // ← How TypeScript finds modules (uses node_modules/)
    "resolveJsonModule": true,     // ← Allow importing .json files (useful for test data!)
    "esModuleInterop": true,       // ← Better importing of CommonJS modules (legacy packages)

    /* === Source & Output === */
    "rootDir": "./",               // ← Source folder (whole project)
    "outDir": "./dist",            // ← Where to save compiled .js (Playwright compiles on-the-fly, so we don't use this)

    /* === Type Checking - STRICT MODE === */
    "strict": true,                // ⭐ ALWAYS true! Enables all strict checks (catches bugs early!)
    "noImplicitAny": true,         // ← Error when TypeScript doesn't know the type (enforces typing)
    "strictNullChecks": true,      // ← null/undefined are separate types (prevents "Cannot read property of null")
    "strictFunctionTypes": true,   // ← Strict function checking (more type safety)
    "noUnusedLocals": true,        // ← Warning when variable is unused (clean code!)
    "noUnusedParameters": true,    // ← Warning when parameter is unused (clean code!)

    /* === Other Options === */
    "skipLibCheck": true,          // ← Don't check types in node_modules/ (faster compilation, 10s → 2s)
    "forceConsistentCasingInFileNames": true,  // ← Error on incorrect casing in imports (Windows vs Linux compatibility)
    "types": ["node"],             // ⭐ IMPORTANT! Include Node.js types (@types/node) - without this TS doesn't understand Node API
    "sourceMap": true              // ← Generate source maps (useful for debugging, shows original .ts in errors)
  },

  /* === What to Include/Exclude === */
  "include": [
    "src/**/*.ts",                 // ← All .ts files in src/ and subfolders (recursive)
    "tests/**/*.ts",               // ← All tests (*.spec.ts, *.test.ts)
    "playwright.config.ts"         // ← Playwright config (explicitly include)
  ],
  "exclude": [
    "node_modules",                // ← Don't compile npm packages (obvious, but explicit is better!)
    "dist",                        // ← Don't compile output (already compiled .js)
    "test-results",                // ← Don't compile test results (JSON, traces generated by Playwright)
    "playwright-report",           // ← Don't compile HTML reports
    "allure-results",              // ← Don't compile Allure results (we'll add this later)
    "allure-report"                // ← Don't compile Allure HTML report
  ]
}
